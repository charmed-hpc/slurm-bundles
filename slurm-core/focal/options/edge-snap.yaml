applications:
  slurmrestd:
    options:
      snapstore-channel: "--edge"
  slurm-configurator:
    options:
      snapstore-channel: "--edge"
  slurmctld:
    options:
      snapstore-channel: "--edge"
  slurmd:
    options:
      snapstore-channel: "--edge"
  slurmdbd:
    options:
      snapstore-channel: "--edge"
